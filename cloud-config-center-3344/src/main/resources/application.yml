server:
  port: 3344

spring:
  application:
    name: cloud-config-center #注册进Eureka服务器微服务名
  cloud:
    config:
      server:
        git:
          uri: https://github.com/yuancen9/springcloud-config.git #github
